# miniboot: tiny Linux bootloader (kexec menu). Built separately from main initramfs.
MODULES=(ext4 sd_mod ahci virtio virtio_ring virtio_blk virtio_pci virtio_scsi nvme)
BINARIES=()
FILES=()
HOOKS=(base miniboot)
COMPRESSION="zstd"
